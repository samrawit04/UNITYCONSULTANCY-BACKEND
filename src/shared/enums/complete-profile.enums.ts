export enum Gender {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
  PREFER_NOT_TO_SAY = 'Prefer not to say',
}

export enum MaritalStatus {
  SINGLE = 'Single',
  MARRIED = 'Married',
  DIVORCED = 'Divorced',
  WIDOWED = 'Widowed',
  PREFER_NOT_TO_SAY = 'Prefer not to say',
}

export enum PreferredPaymentMethod {
  BANK_TRANSFER = 'Bank Transfer',
  TELLBIRR_PAYMENT = 'Telebirr Payment',
}
